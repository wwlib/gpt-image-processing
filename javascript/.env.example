# Copy this file to .env and fill in your real key.
# NEVER commit your real .env.
OPENAI_API_KEY=sk-your-key
# Optionally override model (defaults to gpt-4o-mini for cost efficiency)
MODEL=gpt-4o-mini
# Set to 1 to avoid making a network call (dry run prints payload only)
DRY_RUN=0
